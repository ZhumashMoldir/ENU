# Деректер жиынтығы
data = [45, 55, 60, 75, 80, 85, 90]

# Метрикалық әдіс есепті жасау функциясы
def calculate_metric(data):
    total = sum(data)  # Деректер жиынтығының жалпы санын табу
    count = len(data)  # Деректер жиынтығының элементтерінің санын табу
    if count == 0:
        return None  # Бос жиынтықтың метрикасын есептеу мүмкін емес
    metric = total / count  # Метрикалық әдіс есептеу
    return metric

# Метрикалық әдіс есепті шақырту
result = calculate_metric(data)

# Нәтиже
if result is not None:
    print("Деректер жиынтығының орташа арнайы жартылайтын таңдалуы:", result)
else:
    print("Деректер жиынтығы бос.")
